import React from "react";

const SendButton = () => {
  return (
    <button className="rounded-full border-2 border-white bg-white px-8 py-2 text-sm font-semibold text-black transition duration-300 ease-out hover:bg-black hover:text-white active:scale-95">
      Send
    </button>
  );
};

export default SendButton;
